# The build toolchain (TypeScript, Vite, esbuild) lives in devDependencies, and
# this project is built on the host it deploys to. cPanel exports
# NODE_ENV=production when a Node app's Application mode is Production, which
# makes `npm install` skip devDependencies and the build fail with
# "tsc: command not found". Install them regardless.
include=dev
